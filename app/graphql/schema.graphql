schema {
  query: Query
  mutation: Mutation
}

type Query {
  hello: String
  users: [User]
}

type Mutation {
  hi: String
}

type User {
  id: String
  name: String
  role: String
  phonenum: String
  providers: String
  profile: String
}
